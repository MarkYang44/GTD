# Web Port 8233 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Change the Flask Web service default port from 5000 to 8233 while keeping startup commands and API behavior unchanged.

**Architecture:** Define one host constant and one port constant in `app.py`, then reuse them for both the printed URL and `app.run()`. Update only current user-facing README references; preserve historical design documents.

**Tech Stack:** Python, Flask, unittest, Markdown.

## Global Constraints

- Default host remains exactly `127.0.0.1`.
- Default port becomes exactly `8233`.
- `venv/bin/python app.py` remains the startup command.
- No API route or downloader behavior changes.
- This checkout is not a Git repository; verify with tests and compilation instead of commits.

---

### Task 1: Default Web port and documentation

**Files:**
- Modify: `app.py`
- Modify: `README.md`
- Modify: `tests/test_web_progress.py`

**Interfaces:**
- Produces: `app.WEB_HOST == "127.0.0.1"` and `app.WEB_PORT == 8233`.

- [ ] **Step 1: Write the failing port consistency test**

```python
def test_default_web_port_and_readme_are_8233(self):
    readme = Path("README.md").read_text(encoding="utf-8")
    self.assertEqual(web_app.WEB_HOST, "127.0.0.1")
    self.assertEqual(web_app.WEB_PORT, 8233)
    self.assertIn("http://127.0.0.1:8233", readme)
    self.assertNotIn("http://127.0.0.1:5000", readme)
```

- [ ] **Step 2: Run the focused test and verify RED**

```bash
venv/bin/python -m unittest tests.test_web_progress.WebConfigurationTests -v
```

Expected: failure because `WEB_HOST` and `WEB_PORT` do not exist.

- [ ] **Step 3: Implement the constants and update README**

```python
WEB_HOST = "127.0.0.1"
WEB_PORT = 8233
```

Use the constants in the startup message and `app.run(host=WEB_HOST, port=WEB_PORT, debug=False)`. Replace current README URLs and troubleshooting text from 5000 to 8233.

- [ ] **Step 4: Verify GREEN and full regression**

```bash
venv/bin/python -m unittest tests.test_web_progress.WebConfigurationTests -v
venv/bin/python -m unittest discover -s tests -v
venv/bin/python -m compileall -q app.py downloader.py main.py tests
```

Expected: all commands exit 0.
